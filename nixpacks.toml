[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

[phases.install]
cmds = ["npm install --legacy-peer-deps --include=dev"]

[phases.build]
cmds = ["npm run build:web"]

[start]
cmd = "serve dist --listen tcp://0.0.0.0:$PORT"
